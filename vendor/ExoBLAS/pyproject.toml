[project]
name = "exoblas"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [{ name = "kaylendog", email = "actuallyori@gmail.com" }]
requires-python = "==3.11.*"
dependencies = [
    "exo-lang>=1.0.0",
    "sympy>=1.13.3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
